{
  "_comment": "mssim settings file — all fields may be overridden via CLI flags in run.sh",

  "model": {
    "circuit": "floquet",
    "n_qubits": 6,
    "depth": 2,
    "observable": "zzzzzz",
    "kwargs": {}
  },

  "execution": {
    "engines": ["tn"],
    "n_runs": 1,
    "max_bond_dimension": 4
  },

  "sweep": {
    "n_qubits": [6],
    "depth":    [2]
  },

  "output": {
    "filename": "results/result.jsonl",
    "format":   "jsonl"
  }
}
